(executable
 (public_name bitcask)
 (name main)
 (libraries bitcask fmt checkseum base containers batteries)
 (modes exe byte)
  (ocamlopt_flags (:standard -g))
 )
(env
 (dev
  (flags (:standard -g))
  (ocamlopt_flags (:standard -g))))
